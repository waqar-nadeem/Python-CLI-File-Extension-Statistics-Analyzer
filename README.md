# Python-CLI-File-Extension-Statistics-Analyzer
This repository contains a Python CLI tool that scans directories and generates statistics based on file extensions. Demonstrates directory traversal using os.walk, data aggregation with dictionaries, sorting, and command line parsing with argparse for filesystem analysis and reporting.
